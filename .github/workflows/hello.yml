name: Hello World

on:
  push:
    branches:
      - main

jobs:
  hello_World_Job:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: |
          echo "Hello World !"

  
      

